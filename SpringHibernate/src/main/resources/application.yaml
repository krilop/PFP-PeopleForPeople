spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/PFP
    username: krimlad
    password: krilop
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.Dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true

